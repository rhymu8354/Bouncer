<Window x:Class="Bouncer.Wpf.View.EditNoteWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="{Binding Title}" SizeToContent="WidthAndHeight"
        Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" ShowInTaskbar="False"
>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="10" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="auto" />
        </Grid.ColumnDefinitions>
        <TextBox Text="{Binding Note}" Grid.Row="0" Grid.ColumnSpan="3" Height="200" Width="500" TextWrapping="WrapWithOverflow" AcceptsReturn="True" />
        <Button Grid.Row="2" Grid.Column="0" Content="Cancel" Width="75" Click="OnCancel" IsCancel="True"/>
        <Button Grid.Row="2" Grid.Column="2" Content="OK" Width="75" Click="OnOK" IsDefault="True"/>
    </Grid>
</Window>
